requests>=2.31.0
beautifulsoup4>=4.12.0
python-dotenv>=1.0.0
openpyxl>=3.1.0
pandas>=2.0.0
numpy>=1.24.0
# Pinned to the 1.57.x line the dashboard's modal-dispatch and selection-
# persistence behaviour was developed and browser-tested against. Bump
# deliberately (and re-run the Playwright checks) rather than floating.
streamlit>=1.57.0,<1.58.0
plotly>=5.20.0
